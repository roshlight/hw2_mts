# Real-Time Fraud Detection System (MLOps HW2)

Система обнаружения мошеннических транзакций в реальном времени с использованием Kafka, CatBoost и PostgreSQL.

## 🌟 Основные возможности 
- **Стриминг:** Имитация потока транзакций через Kafka.
- **ML Scoring:** Обработка данных "на лету" моделью CatBoost.
- **Хранение (Витрина):** Результаты автоматически сохраняются в базу данных PostgreSQL.
- **Аналитический Dashboard:** - Просмотр 10 последних фродовых транзакций из БД.
  - Гистограмма распределения скоров для последних 100 транзакций.

## 🏗️ Архитектура системы
1. **`interface`**: Streamlit-приложение для отправки CSV в Kafka и визуализации данных из БД.
2. **`fraud_detector`**: Python-сервис, выполняющий препроцессинг и инференс ML-модели.
3. **`result_saver`**: Консьюмер, который перекладывает предсказания из топика `scoring` в таблицу `scores` базы PostgreSQL.
4. **Инфраструктура**: Kafka, Zookeeper, Kafka UI (мониторинг), PostgreSQL.

## 🚀 Быстрый старт

### Требования
- Docker & Docker Compose

### Запуск
```bash
docker-compose up --build -d